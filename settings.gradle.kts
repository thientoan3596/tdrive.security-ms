rootProject.name = "security-ms"
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        id("org.gradle.plugin.management") version "0.13.1"
    }
}
